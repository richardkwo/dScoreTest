% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hte.R
\name{debias_hte_conditional}
\alias{debias_hte_conditional}
\title{Customized debiasing for \code{\link[=hte_test_conditional]{hte_test_conditional()}}}
\usage{
debias_hte_conditional(
  h.hat,
  X.debias,
  fit.debias,
  predict_fun,
  weight_fun,
  wls_method,
  arg.wls_method,
  randomized = FALSE
)
}
\arguments{
\item{h.hat}{Object of class \code{hunt} produced by \code{\link[=hunt_optimal]{hunt_optimal()}},
\code{\link[=hunt_wls]{hunt_wls()}} or \code{\link[=hunt_vanilla]{hunt_vanilla()}}, where \code{h.hat$h(X)} is \eqn{\hat{h}(t, Z)} for
\eqn{X=[t, Z]}.}

\item{X.debias, fit.debias, predict_fun, weight_fun, wls_method, arg.wls_method}{See \code{\link[=dScoreTest]{dScoreTest()}} for details.
Argument \code{arg.wls_method} must contain field \code{S} that defines the model space.}

\item{randomized}{If \code{FALSE} (default), the propensity
\eqn{e(Z) = \mathbb{E}[T \mid Z]} is estimated with
\code{grf::probability_forest}. If \code{TRUE}, \code{T} is assumed
randomized (independent of \code{Z}), so \eqn{e(Z)} is taken to be
the constant \code{mean(T)} on the debiasing sample, fitted upfront
without cross-fitting.}
}
\value{
A list with elements:
\describe{
\item{\code{m.h.fit}}{The null model fitted (over all columns of \code{X})
to project and debias \eqn{\hat{h}}.}
\item{\code{h}}{The debiased hunt function \eqn{\hat{h} - \hat{m}_{\hat{h}}}
with signature \code{h(X)}.}
}
}
\description{
For a hunt \eqn{\hat{h}(t,z)}, it can
be rewritten as \deqn{\hat{h}(t,z) = \hat{h}_0(z) + t \cdot \hat{h}_{\Delta}(z),}
where \eqn{\hat{h}_{\Delta}(z) := \hat{h}(1,z) - \hat{h}(0,z)}.
Let the projection of \eqn{\hat{h}(t,z)} onto the null space be
\deqn{m_{\hat{h}}(t,z) = m_0(z) + t \cdot m_{\Delta}(z_{S^c}).}
This function returns the debiased hunt function \eqn{\hat{h} - \hat{m}_{\hat{h}}}.
}
\details{
Function \eqn{\hat{m}_{\Delta}} is fitted by minimizing
\deqn{\sum_i (T_i - \hat{e}(Z_i))^2 \, (\hat{h}_{\Delta}(Z_i) - m_{\Delta}(Z_{i,S^c}))^2,}
where \eqn{e(z):= \mathbb{E}(T \mid Z=z)}.
The debiased hunt function is given by
\deqn{(\hat{h} - \hat{m}_{\hat{h}})(t,z) = (t - \hat{e}(z))\,(\hat{h}_{\Delta}(z) - \hat{m}_{\Delta}(z_{S^c})).}
}
\keyword{internal}
